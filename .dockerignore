# deps + build artifacts (rebuilt inside the image)
node_modules
**/node_modules
.next
**/.next
.turbo
**/.turbo
dist
**/dist
out
**/out
**/*.tsbuildinfo

# Go API มี Dockerfile ของตัวเอง (context = apps/api)
apps/api

# vcs + local
.git
.gitignore
.claude
scratchpad
deploy

# secrets (ให้ค่าผ่าน compose/env เท่านั้น)
**/.env
**/.env.local
**/.env*.local

# misc
**/*.log
docs
*.md
!README.md
