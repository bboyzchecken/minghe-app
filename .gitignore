# dependencies
node_modules/
.pnpm-store/

# builds
.next/
.turbo/
dist/
out/
*.tsbuildinfo
next-env.d.ts

# env & secrets
.env
.env.local
.env*.local
# ไฟล์ credential ที่ดาวน์โหลดจาก Google Cloud Console มาทั้งก้อน
# (GitHub push protection บล็อกให้อยู่แล้ว แต่กันไว้ตั้งแต่ต้นทางดีกว่า)
client_secret_*.json
*-service-account.json

# Go
apps/api/artifacts/
*.exe

# misc
.DS_Store
*.log
.vercel

# claude code runtime
.claude/scheduled_tasks.lock
.claude/settings.local.json
