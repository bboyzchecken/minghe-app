# MingHe — แผนงานรอบ 2 (เอกสารสเปกใหม่ 4 ฉบับ + feedback หน้าแรก)

> ที่มา: `~/kami/` — `MingHe_TextForLandingPage.pdf` · `Minghe_Soul&SenseOfLaunguage.pdf` ·
> `MingHe_DataSpecInterpretationLogic_LLMDecisionFramework.pdf` · `SpecificationDirection.pdf`
> วันที่: 7 ก.ย. 2026 · สถานะ: **แผน ยังไม่เริ่มโค้ด**
> คู่กับ: [แผน UAT รอบ 1](uat-2026-08-01-feedback-plan.md) · [ชีทคำถามค้าง](uat-2026-08-01-ask-ajarn-may.md)

---

## 0. ข้อค้นพบที่เปลี่ยนขนาดงานทั้งหมด

เอกสารใหม่ทั้ง 4 ฉบับตั้งอยู่บนสมมติฐานว่า *"Backend คำนวณ deterministic ทั้งหมด แล้วส่ง structured JSON ให้ LLM reasoning"*
แต่ระบบจริงวันนี้ **ไม่เป็นแบบนั้นเลยสักข้อ** — ตรวจจากโค้ดจริง:

| # | ข้อเท็จจริง | หลักฐาน |
|---|---|---|
| 1 | **ไม่มี LLM ทำงานอยู่ในระบบจริงเลย** | [`apps/app/lib/report.ts:8-9`](../apps/app/lib/report.ts#L8) import แบบ subpath พร้อมคอมเมนต์ *"เพื่อ 'ไม่' ลาก @anthropic-ai/sdk เข้ามาใน bundle"* แล้ว hardcode `polishedByAI: false` · `generateReport()` ที่เรียก Claude อยู่ใน [`packages/report/src/index.ts:16`](../packages/report/src/index.ts#L16) — **ไม่มีใครเรียก** |
| 2 | **Go API ไม่มีเครื่องคำนวณปาจื้อเลย** | `apps/api/pkg/core/` มีแค่ `config.go` · Go ทั้งหมด 5,954 บรรทัดเป็น auth / order / billing / org |
| 3 | **หน้าเว็บเป็น static export → คำนวณในเบราว์เซอร์ผู้ใช้** | [`apps/app/next.config.mjs`](../apps/app/next.config.mjs) `output: 'export'` + `transpilePackages: ['@minghe/core','@minghe/report']` |

**ผลที่ตามมา:** PART A (22 field groups) + PART B (19 reasoning steps) **ไม่ใช่ "เพิ่มฟีเจอร์" แต่คือสร้างชั้นใหม่ทั้งชั้น**
และขัดกับข้อสรุป **Q0-1b** เดิม (*"Go API เป็นเจ้าของข้อมูลเจ้าเดียว"*) ที่ยังไม่เคยครอบคลุมถึงชั้นคำนวณ

---

## 1. การตัดสินใจที่ได้แล้ว (7 ก.ย. 2026)

| # | คำถาม | คำตอบ |
|---|---|---|
| **D-1** | เดินสาย 子平法 เป็นแกนหลัก + ดาว 神煞 เป็นแค่ตัวขยายบริบท? | ⏳ **ยังไม่ยืนยัน — ขอคุยกับ อ.เม ก่อน** |
| **D-2** | text หน้าแรกชุดใหม่จัดวางอย่างไร | ✅ **แตกเป็น 3 ที่** |
| **D-3** | engine ปาจื้อไปอยู่ที่ไหน | ✅ **เขียนใหม่ใน Go** |
| **D-4** | สูตรที่ยังขาด (% สิบเทพ / threshold / Five Structures / Profile Roles / 大運) | ✅ **ร่างสูตรให้ก่อน แล้วส่ง อ.เม ตรวจ** |

> ⚠️ **D-1 ยังไม่ปิด = ต้องกันงานที่ขึ้นกับมันออกเป็นกลุ่มแยก** ไม่ให้บล็อกงานอื่น (ดู §2)

---

## 2. งานแบ่งตามว่า "รอ D-1 หรือไม่"

### 🟢 ไม่ต้องรอ D-1 — เป็นฐานร่วมของทั้งสองสาย

4 เสา · ก้านแฝง · สิบเทพ · การกระจายธาตุ · เวลาสุริยะจริง · 合沖刑害 · โครงหน้าเว็บ
→ **Phase 1 และ Phase 2 เดินได้ทันที**

### 🔴 ต้องรอ D-1 — เปลี่ยนคำตอบไปคนละทาง

| งาน | ถ้าเลือก 子平法 | ถ้าเลือก 祿命法 |
|---|---|---|
| [`apps/app/lib/stars.ts`](../apps/app/lib/stars.ts) | เขียน gatekeeper 4 ชั้นครอบ (mute เมื่อโดนชง / ล็อก scope ตามเสา / แปลตาม favorable) | เก็บแบบ `active: true/false` เดิม แล้วขยายให้ครบชุดดาว |
| `readFace()` ([`llm.ts:91`](../packages/report/src/llm.ts#L91)) | **ตัดทิ้ง** — 面相 เป็นศาสตร์ที่สาม ไม่ใช่ทั้งสองสาย | เก็บไว้เป็นบริการเสริม + disclaimer |
| ลำดับความสำคัญในรายงาน | ตาม B20: Structure > Capacity > 5 Structures > Profile > Pillar > Ten God > **ดาวท้ายสุด** | ดาวขึ้นมาเป็น headline ได้ |
| โทนภาษา | บังคับ **Anti-Fatalism Rule** — ห้าม "ดวงกำหนดมาแล้ว / ดวงแตก / อาภัพ / ซวย" | ใช้ภาษาทำนายได้ตามธรรมเนียม |
| การ์ด 10 ก้านวัน (เนื้อหาชุดใหม่) | ใช้ได้เลย — เขียนแบบ 子平法 อยู่แล้ว ไม่พูดถึงดาวเลย | **ต้องรื้อเขียนใหม่** |
| การตลาด / pricing | ขายเป็น "ที่ปรึกษาเชิงกลยุทธ์" | ขายเป็น "บริการดูดวงเชิงลึก" |

**ความเสี่ยงที่ต้องบอกให้ชัด:** ข้อความหน้าแรกชุดใหม่ทั้ง 10,827 อักษร **เขียนบนสมมติฐาน 子平法 ไปแล้ว**
ถ้า อ.เม เลือกอีกทาง งาน Phase 1 จะต้องรื้อเนื้อหา (โครงหน้าไม่ต้องรื้อ)
→ **ทางกัน:** Phase 1 ทำโครงหน้า + routing ก่อน แล้วค่อยเทเนื้อหาลงเมื่อ D-1 ปิด

---

## 3. Phase 0 — ปลดล็อกการตัดสินใจ (ไม่แตะโค้ด)

| ID | งาน | ผลลัพธ์ | ยาก-ง่าย |
|---|---|---|---|
| **P0-1** | **เอกสารตัดสินสายวิชา ให้ อ.เม** — เปรียบเทียบ 子平法 vs 祿命法 เชิงวิศวกรรม (ไม่ใช่เชิงความเชื่อ) + ตารางผลกระทบตาม §2 + คำถาม 3 ข้อที่ต้องตอบ | `docs/ask-ajarn-may-round2-school.md` (พิมพ์หน้าเดียว) | S |
| **P0-2** | **ร่าง Ming He Calculation Methodology v0.1** — สูตร 5 กลุ่มที่ PDF อ้างถึงแต่ไม่มี | `docs/calculation-methodology-v0.1.md` | M |
| **P0-3** | ล็อกคิว review กับ อ.เม (คำถาม **A4** เดิมที่ค้างมาตั้งแต่ ส.ค. ยังไม่เคยตอบ) | ตารางรอบส่ง-ตรวจ | XS |

**P0-2 ต้องร่างสูตรอะไรบ้าง**

| กลุ่ม | สเปกอ้างถึง | สถานะจริง | ตั้งต้นจากอะไรได้ |
|---|---|---|---|
| % ของสิบเทพ | A11 *"จนกว่า Ming He Calculation Methodology จะกำหนดสูตรอย่างเป็นทางการ"* | เอกสารนั้นไม่มีอยู่จริง · [`report.ts:40`](../apps/app/lib/report.ts#L40) ใช้ weight ÷ total ของ top 5 ไปก่อน | ถ่วงน้ำหนักก้านฟ้า 1.0 + ก้านแฝงตาม 本氣/中氣/餘氣 ที่มีใน [`types.ts:24`](../packages/core/src/types.ts#L24) |
| threshold `clearly dominates` | B8 *"LLM ห้ามตั้ง threshold เอง"* | ไม่มีใครให้ | เสนอ: อันดับ 1 นำอันดับ 2 ≥ 20% ของคะแนนเต็ม |
| Five Structures | A7 (Companion/Output/Wealth/Influence/Resource) | มีแต่หน้าตา JSON ไม่มีกฎ derive | จับคู่จากสิบเทพที่ [`constants.ts`](../packages/core/src/constants.ts) มีครบแล้ว → รวมเป็น 5 กลุ่ม |
| Profile Roles 5 ตัว | A12 (Main/Sub/Base/Social/Intimate) | ไม่มีกฎ | Base←เสาวัน · Social←เสาปี · Intimate←เสายาม · Main←สูงสุดรวม · Sub←อันดับ 2 (ตาม A15 pillar context) |
| 大運 (ดวงใหญ่ 10 ปี) | ทั้ง DataSpec และ **A5 momentum** ในชีทคำถามค้างเดิม | **`packages/core` ยังไม่มีเลย** — บล็อก F-21/F-22/F-26 มาตั้งแต่ ส.ค. | สูตรมาตรฐาน: หยางชาย/หยินหญิง=เดินหน้า · นับวันถึงเจี๋ยชี่ ÷ 3 = อายุเริ่มต้น |

> P0-2 ทำให้ **F-21 / F-22 / F-26 ที่ค้างมา 5 สัปดาห์ปลดล็อกพร้อมกัน 3 ข้อ**

---

## 4. Phase 1 — หน้าแรกแตก 3 ที่ (D-2 · ไม่รอใคร)

**ตัวเลขตั้งต้น** (นับอักษรไทย)

| | อักษรไทย | ปลายทาง |
|---|---|---|
| หน้าแรกปัจจุบันทั้งหน้า | 1,563 | — |
| บล็อก Strip / ภาพรวม 3 ชั้น | 1,580 | → หน้าแรก (**ย่อเหลือ ~600–700**) |
| การ์ด 10 ก้านวัน | 6,531 | → `/elements/[stem]` **10 หน้า** |
| จดหมายทีมซินแส | 2,716 | → `/about` |
| **รวมชุดใหม่** | **10,827** (อ่าน ~9–11 นาที) | |

| ID | งาน | ยาก-ง่าย |
|---|---|---|
| **P1-1** | ย่อบล็อก Strip เหลือ 3 หัวข้อ (เสาเดือน / ดิถีแข็ง-อ่อน / สิบเทพ+โครงสร้าง) + ลิงก์ "อ่านวิธีอ่านดวงแบบเต็ม →" ใน [`page.tsx`](../apps/app/app/page.tsx) | S |
| **P1-2** | `/elements/[stem]` 10 หน้า จากการ์ด 10 ก้านวัน — ได้ SEO 10 เด้ง (คนค้น "ดิถีไม้หยาง คือ") | M |
| **P1-3** | `/about` + จดหมายทีมซินแส | S |
| **P1-4** | **แก้ความขัดแย้งในจดหมาย** — จดหมายเขียนว่า *"ความเข้าใจผิด...โดยเฉพาะการนำ AI มาใช้ดูดวงเพียงอย่างเดียว"* แต่ MingHe จะใช้ LLM เขียนรายงาน → เติมย่อหน้าว่า **"เราไม่ให้ AI แตะการคำนวณเลย — AI แปลผลลัพธ์ที่ซินแสกำหนดกฎไว้"** ซึ่งตรงกับ B0 ของ DataSpec พอดี | XS |
| **P1-5** | **ตัดสิน 5 ธาตุ vs 10 ก้านวัน** — หน้าแรกตอนนี้มี `ElementGallery` เป็น **5 ธาตุ** ชุดใหม่เป็น **10 ก้านวัน** คนละ granularity ใส่ทั้งคู่ผู้ใช้จะสับสนว่าตัวเองเป็นอะไร | XS (ตัดสิน) + S (ทำ) |

---

## 5. Phase 2 — Go engine (D-3 · งานใหญ่ที่สุด)

**ขนาดงานจริง:** `packages/core/src` = **2,135 บรรทัด** · `packages/report/src` = 954 บรรทัด · เทสต์ที่ต้องไม่ให้พัง = 530 บรรทัด

| ID | งาน | ยาก-ง่าย |
|---|---|---|
| **P2-0** | ⚠️ **ทำก่อนพอร์ตบรรทัดแรก** — สร้าง golden test vectors: รันดวงตัวอย่าง ~500 ใบผ่าน `packages/core` แล้ว dump JSON เป็นไฟล์ fixture · Go ต้องผ่าน fixture เดียวกันทุกใบ **นี่คือเงื่อนไขบังคับ ไม่ใช่ nice-to-have** — เป็นสิ่งเดียวที่กันตัวเลขเพี้ยนเงียบ ๆ ตอนพอร์ต | M |
| **P2-1** | พอร์ตฐาน: `constants` · `solar-time` (真太陽時 + Equation of Time) · `bazi` (4 เสา + ก้านแฝง + สิบเทพ)<br>✅ **ปฏิทินจีนไม่ต้องเขียนเอง** — engine พึ่งไลบรารีภายนอกตัวเดียวคือ `lunar-typescript` และผู้เขียนคนเดียวกันมี [`lunar-go`](https://github.com/6tail/lunar-go) → <span class="cjk">24 節氣</span> / ก้าน-กิ่ง / ปฏิทินจันทรคติ ยกมาได้ **ความเสี่ยงต่ำกว่าที่ประเมินไว้ครั้งแรกมาก** | L |
| **P2-2** | พอร์ต `wuxing` — 得令/得地/得勢 + 喜用神 ([`wuxing.ts:5-13`](../packages/core/src/wuxing.ts#L5) อธิบายสูตรไว้ครบแล้ว) | M |
| **P2-3** | พอร์ต `compatibility` (798 บรรทัด — ใหญ่สุด) + `industry` | L |
| **P2-4** | **ของใหม่ที่ TS ยังไม่มี**: 格局 classical pattern · 合沖刑害 ภายในดวงเดียว (ตอนนี้มีแค่ระหว่างคน↔องค์กร) · mutation/recalculation engine (A17) · **大運** | XL |
| **P2-5** | สัญญา JSON ตาม PART A ครบ 22 กลุ่ม + `calculation_version` (A1) | M |
| **P2-6** | ปลด client-side calculation ออกจาก `apps/app` → เรียก Go แทน · **ต้องตัดสินว่ายังคง `output: 'export'` ได้ไหม** | M |

---

## 6. Phase 3 — LLM Interpretation Service (รอ Phase 2 + D-1)

| ID | งาน | ยาก-ง่าย |
|---|---|---|
| **P3-1** | บริการ interpretation ฝั่ง server (ตอนนี้ไม่มีที่ให้ API key อยู่เลย) | M |
| **P3-2** | Interpretation Constitution เป็น system prompt จาก `Soul` PDF | M |
| **P3-3** | reasoning 19 steps (B1) + core model + 3–5 hypotheses พร้อม evidence 2 ชั้น (B30) | L |
| **P3-4** | 8 career dimensions (B32) | M |
| **P3-5** | Contradiction Resolution Engine (B25/B26) | M |

**ความเสี่ยงที่ต้องวัดก่อนสร้างจริง:** reasoning 19 steps + hypotheses × evidence 2 ชั้น = prompt ยาวมาก
ประเมินหยาบ **30–60 วินาที และ ~15–40 บาทต้นทุน LLM ต่อรายงาน 1 ใบ** เทียบราคาขาย **199 บาท**
→ ต้องทำ spike วัดของจริงก่อน ไม่ใช่สร้างเสร็จแล้วค่อยรู้

---

## 7. สิ่งที่ต้องระวัง

| # | เรื่อง | ทำไม |
|---|---|---|
| R-1 | **พอร์ต Go แล้วตัวเลขเพี้ยนเงียบ ๆ** | ปฏิทินจีนยกมาได้ (ดู P2-1) แต่**กฎให้คะแนน** 得令/得地/得勢 + สมพงษ์ 798 บรรทัด ยังต้องเขียนมือ · ผิดจุดเดียว = รายงานยังออกปกติ ไม่มีอะไรฟ้อง แค่คำตอบผิด → **P2-0 บังคับ**<br>📄 เทียบทางเลือกแบบละเอียด (สำหรับคนไม่รู้เทค): [เอกสารตัดสินใจเรื่อง engine](https://claude.ai/code/artifact/b4d1006a-0af6-4b8c-a748-22c76764a043) |
| R-2 | **SpecDirection ข้อ 13 ขัดกับ B0/B37 ของตัวเอง** | ข้อ 13 บอก *"LLM มีสิทธิ์ไม่เห็นด้วยกับ rule-based conclusion"* แต่ B0/B37 ห้าม LLM สรุปนอกกฎ — **ต้องเลือกข้างเดียว** ไม่งั้น dev ตีความคนละทาง |
| R-3 | **Persona "อดีตที่ปรึกษา McKinsey วัย 42 ปี ประสบการณ์ปาจื้อ 35 ปี"** | เป็นบุคคลสมมติ · ใช้เป็น system prompt ได้ แต่ **ห้ามแสดงต่อผู้ใช้ว่าเป็นคนจริง** — ผิดทั้ง PDPA/โฆษณาเกินจริง และขัดร่างเงื่อนไขที่ทำไว้ที่ [`apps/app/lib/legal/`](../apps/app/lib/legal/) |
| R-4 | **ต้นทุน LLM ต่อใบ vs ราคาขาย 199** | ดู §6 |
| R-5 | **เนื้อหาหน้าแรกชุดใหม่ผูกกับ 子平法 ไปแล้ว** | ถ้า D-1 ออกอีกทาง ต้องรื้อเนื้อหา → Phase 1 ทำโครงก่อน เทเนื้อหาทีหลัง |
| R-6 | **F-24 rule table ยังรอ อ.เม มาตั้งแต่ ส.ค.** | DataSpec **B34 (Strength→Blind Spot)** ให้กรอบมาแล้ว แต่ยังไม่ใช่รายการ 15–20 ข้อพร้อมเงื่อนไข → ยังค้าง |
| R-7 | **การ์ด 10 ก้านวันซ้ำกับ ElementGallery 5 ธาตุ** | ดู P1-5 |

---

## 8. ข่าวดี — เอกสารใหม่ปิดคำถามค้างเก่าให้ 2 ข้อ

| คำถามค้างเดิม | เอกสารใหม่ตอบให้แล้ว |
|---|---|
| **A2** *"ขอย่อหน้าตัวอย่างที่เขียนถูกใจ"* (F-23) — เคยระบุว่า **มีค่าที่สุดในลิสต์ทั้งหมด** | ✅ `Soul` หน้า 4–6 ให้ตัวอย่าง ❌/✅ ครบ **3 เคส** (桃花 / 日支沖 / 身弱) + §5 Consultant's Voice → **ใช้เป็น style reference ได้เลย** |
| **A1** *"คำว่า 'ธาตุโปรด' ใช้ได้ไหม"* (F-09) | ✅ เอกสารใช้ **用神 = Leverage Point / Key Success Factor** |

และ grep คำต้องห้ามตาม Anti-Fatalism Rule ทั้ง repo → เจอแค่ 3 จุด และ**ทั้ง 3 จุดคือคำสั่งห้าม**ใน
[`llm.ts:52,55,111`](../packages/report/src/llm.ts#L52) — **ไม่มีภาษา fatalistic รั่วออกหน้าเว็บเลย** จุดนี้สอบผ่านอยู่แล้ว

---

## 9. ลำดับที่แนะนำ

```
สัปดาห์นี้     P0-1 (เอกสารให้ อ.เม)  ─┐
              P0-2 (ร่างสูตร v0.1)    ─┼─→ ส่ง อ.เม พร้อมกันรอบเดียว
              P0-3 (ล็อกคิว review)   ─┘

ขนานกันไป      P1-1…P1-5 โครงหน้าแรก (ไม่รอใคร)
              P2-0 golden test vectors (ไม่รอใคร · ต้องทำก่อนพอร์ต)

หลัง อ.เม ตอบ  D-1 ปิด → เทเนื้อหาลง Phase 1 + เริ่ม Phase 2 เต็มตัว
              Phase 3 หลัง Phase 2 มี PART A ครบ
```

**ทางวิกฤต (critical path) = คำตอบของ อ.เม** — ไม่ใช่โค้ด
